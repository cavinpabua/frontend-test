import { theme } from "antd";

const themeConfig = {
  token: {
    colorPrimary: "#3e1f47",
  },
  algorithm: theme.defaultAlgorithm,
  components: {
    Layout: {
      colorBgBody: "#f7f7f7",
    },
  },
};

export default themeConfig;
